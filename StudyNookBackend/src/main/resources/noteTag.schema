CREATE TABLE note_tag(
id BIGINT AUTO_INCREMENT NOT NULL PRIMARY KEY,
user_tag_id BIGINT,
note_id BIGINT,
name VARCHAR(50),
FOREIGN KEY (user_tag_id) REFERENCES user_tag (id) ON DELETE CASCADE,
FOREIGN KEY (note_id) REFERENCES note (id) ON DELETE CASCADE
);